import random


def number_guessing_game():
    G=input("May I ask you question?")

    input(G)
    print("I have selected a random number between 1 and 200. Can you guess it?")

    # Generate a random number between 1 and 200
    secret_number = random.randint(1, 200)

    attempts = 5
    while True:
        # Get the player's guess
        guess = int(input("Enter your guess: "))

        # Increment the attempts counter
        attempts += 1

        # Check if the guess is correct
        if guess == secret_number:
            print("Congratulations! You guessed the correct number in {attempts} attempts.")
            break
        elif guess < secret_number:
            print("Too low. Try again.")
        elif guess==(attempts==5):
            print("my gussed number is")
        elif guess == int(187):
            print("Perfect")
        else:
            print("Too high. Try again.")


# Run the game
number_guessing_game()
